import React from "react";
import ReactDOM from "react-dom/client"; // SOLO esta importación es necesaria
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);